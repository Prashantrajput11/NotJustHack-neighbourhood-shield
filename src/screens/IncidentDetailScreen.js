import { StyleSheet, Text, View } from "react-native";
import React from "react";

const IncidentDetailScreen = () => {
	return (
		<View>
			<Text>IncidentDetailScreen</Text>
		</View>
	);
};

export default IncidentDetailScreen;

const styles = StyleSheet.create({});
